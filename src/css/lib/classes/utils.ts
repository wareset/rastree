// import { replace } from 'wareset-utilites'

// import { trimCss } from '../trim-css'
import { NULL } from '../../../lib/classes/utils'

export { NULL }
export const EMPTY = ''
export const SPACE = ' '

// export const OPTIONS = {
//   indent: '\t',
//   concise: false,
//   global: true,
//   scoper: '[scoped]',
//   minify: false
// }

// export const replaceLineTerminators = (s: string): string =>
//   replace(s, /\r?\n/g, SPACE)

// export const normalizeCss = (s: string): string =>
//   replaceLineTerminators(trimCss(s))
